{
  // desktop-live2d config layers:
  // 1. Code defaults live in apps/desktop-live2d/shared/defaultUiConfig.js
  // 2. User overrides live in $YACHIYO_HOME/config/desktop-live2d.json
  // 3. Runtime window memory lives in $YACHIYO_HOME/data/desktop-live2d/window-state.json
  // This template is only used to create the user override file on first run.
  // Override only values that should differ from code defaults.
  "window": {
    // Slightly larger default desktop avatar window.
    "width": 380,
    "height": 600,
    "minWidth": 240,
    "minHeight": 360,
    "compactWidth": 320,
    "compactHeight": 600
  },
  "voice": {
    "path": "electron_native"
  },
  "debug": {
    // Mouth Tuner 调试面板。visible 控制是否显示按钮/面板，
    // enabled 控制是否默认启用覆盖。默认都关闭，避免影响日常使用。
    "mouthTuner": {
      "visible": false,
      "enabled": false
    }
  }
}
