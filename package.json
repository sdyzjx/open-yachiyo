{
  "name": "open-yachiyo",
  "version": "0.1.0",
  "private": true,
  "main": "apps/desktop/main.js",
  "type": "commonjs",
  "scripts": {
    "dev": "node apps/gateway/server.js",
    "start": "node apps/gateway/server.js",
    "desktop:up": "node scripts/desktop-up.js",
    "desktop:smoke": "node scripts/desktop-live2d-smoke.js",
    "desktop:dev": "electron apps/desktop/main.js",
    "desktop:start": "electron apps/desktop/main.js",
    "desktop:pack:win": "electron-builder --win nsis --publish never",
    "desktop:dist:win": "electron-builder --win nsis --x64 --publish never",
    "live2d:import": "node scripts/live2d-import.js",
    "test": "node --test \"test/**/*.test.js\"",
    "test:ci": "node --test $(find test -name '*.test.js' ! -name 'voiceAdapter.test.js')"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "dotenv": "^17.3.1",
    "express": "^4.19.2",
    "pixi-live2d-display": "^0.4.0",
    "pixi.js": "^6.5.10",
    "uuid": "^11.0.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "electron": "^30.5.1",
    "electron-builder": "^24.13.3",
    "yaml": "^2.8.2"
  },
  "build": {
    "appId": "com.yachiyo.desktop",
    "productName": "Open Yachiyo",
    "asar": true,
    "directories": {
      "output": "dist",
      "buildResources": "assets"
    },
    "files": [
      "apps/**/*",
      "assets/**/*",
      "config/**/*",
      "scripts/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "resources/bin",
        "to": "bin",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "${productName}-Setup-${version}-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "allowToChangeInstallationDirectory": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "runAfterFinish": true,
      "artifactName": "${productName}-Setup-${version}-${arch}.${ext}"
    }
  }
}
